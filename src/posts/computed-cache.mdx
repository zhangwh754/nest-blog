---
title: computed-cache
description: 简单利用闭包缓存计算结果
type: 代码片段
---

## 标题

你好世界

```js
const mult = () => {
  const cache = {}

  return async function (...args) {
    const key = args.join(',')

    if (cache[key]) {
      return cache[key]
    }

    const result = args.reduce(async (pre, cur) => pre * cur, 1)

    cache[key] = result

    return result
  }
}

const foo = mult()

console.log(foo(1, 2, 3))
console.log(foo(1, 2, 3))
console.log(foo(1, 2, 3))
console.log(foo(1, 2, 3))
```

`利用闭包缓存计算结果`